<h1>Editing: <%= @todo.title%></h1>

<hr />

<%= form_for @todo do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title, required: true %>
  <br />
  <%= f.submit %>
<% end %>

<%= link_to 'Cancel', todo_path(@todo) %>

